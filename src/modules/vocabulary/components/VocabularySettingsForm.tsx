"use client";

import { FormEvent, useState } from "react";

export function VocabularySettingsForm({ initial }: { initial: { dailyGoal: number; maxSessionSize: number; showTranscription: boolean; autoplayAudio: boolean; pronunciationVariant: string; includeDifficultWords: boolean; dailyReminderEnabled: boolean; translationLanguage: string } }) {
  const [settings, setSettings] = useState(initial); const [status, setStatus] = useState<string | null>(null);
  async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setStatus(null); const response = await fetch("/api/profile/vocabulary/settings", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(settings) }); setStatus(response.ok ? "Settings saved." : "Unable to save settings."); }
  return <form onSubmit={(event) => void save(event)} className="mt-8 grid gap-5 rounded-2xl border border-slate-200 bg-white p-6 md:grid-cols-2"><label className="text-sm font-medium">Daily goal<input type="number" min="1" max="100" value={settings.dailyGoal} onChange={(event) => setSettings({ ...settings, dailyGoal: Number(event.target.value) })} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2" /></label><label className="text-sm font-medium">Maximum words per session<input type="number" min="1" max="50" value={settings.maxSessionSize} onChange={(event) => setSettings({ ...settings, maxSessionSize: Number(event.target.value) })} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2" /></label><label className="text-sm font-medium">Translation language<input value={settings.translationLanguage} onChange={(event) => setSettings({ ...settings, translationLanguage: event.target.value })} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2" /></label><label className="text-sm font-medium">Pronunciation<select value={settings.pronunciationVariant} onChange={(event) => setSettings({ ...settings, pronunciationVariant: event.target.value })} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2"><option value="BOTH">British and American</option><option value="BRITISH">British</option><option value="AMERICAN">American</option></select></label>{[["showTranscription", "Show transcription"], ["autoplayAudio", "Autoplay audio"], ["includeDifficultWords", "Include difficult words"], ["dailyReminderEnabled", "Show daily reminder"]].map(([key, label]) => <label key={key} className="flex items-center gap-2 text-sm font-medium"><input type="checkbox" checked={Boolean(settings[key as keyof typeof settings])} onChange={(event) => setSettings({ ...settings, [key]: event.target.checked })} />{label}</label>)}<div className="md:col-span-2"><button className="rounded-lg bg-blue-700 px-4 py-2 font-semibold text-white hover:bg-blue-800">Save settings</button>{status ? <p className="mt-3 text-sm text-slate-700" role="status">{status}</p> : null}</div></form>;
}
