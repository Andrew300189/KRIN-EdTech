import Link from "next/link";
import { notFound } from "next/navigation";
import { requireAuth } from "@/core/server/session";
import { canAccessLesson } from "@/modules/courses/services/lesson-access.service";
import { getPublishedCourseBySlug, getPublishedLevelWithCourses, listLessonProgressByLessonIds } from "@/modules/courses/services/content.service";

function strings(value: unknown) {
  return Array.isArray(value) ? value.filter((item): item is string => typeof item === "string") : [];
}

export default async function CourseOrLevelPage({ params }: { params: Promise<{ level: string }> }) {
  const { level: slug } = await params;
  const dbLevel = await getPublishedLevelWithCourses(slug);
  if (dbLevel) return <main className="mx-auto max-w-6xl px-6 py-12"><Link href="/courses" className="text-sm font-semibold text-blue-700 hover:underline">← All courses</Link><header className="mt-5"><p className="text-sm font-semibold uppercase tracking-wide text-blue-700">CEFR level</p><h1 className="mt-2 text-4xl font-bold text-slate-900">{dbLevel.code} — {dbLevel.title}</h1>{dbLevel.description ? <p className="mt-3 max-w-3xl text-slate-600">{dbLevel.description}</p> : null}</header>{dbLevel.courses.length === 0 ? <p className="mt-8 rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-7 text-slate-600">Published courses for this level are being prepared.</p> : <section className="mt-8 grid gap-5 md:grid-cols-2 xl:grid-cols-3" aria-label={`${dbLevel.code} courses`}>{dbLevel.courses.map((course) => <article key={course.slug} className="flex flex-col rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex gap-2 text-xs font-semibold"><span className="rounded-full bg-slate-100 px-3 py-1 text-slate-700">{course.category.title}</span><span className="rounded-full bg-amber-50 px-3 py-1 text-amber-800">{course.accessPlan === "FREE" ? "Free" : course.accessPlan === "PREMIUM" ? "Premium" : "Corporate"}</span></div><h2 className="mt-4 text-xl font-bold text-slate-900">{course.title}</h2><p className="mt-3 flex-1 text-sm text-slate-600">{course.shortDescription}</p><p className="mt-5 text-sm text-slate-500">{course.lessonCount} lessons · {course.estimatedDuration || "—"} min</p><Link href={`/courses/${course.slug}`} className="mt-5 inline-flex min-h-11 items-center justify-center rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-800 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">View course</Link></article>)}</section>}</main>;

  const course = await getPublishedCourseBySlug(slug);
  if (!course) notFound();
  const authenticated = await requireAuth();
  const lessonIds = course.modules.flatMap((module) => module.lessons.map((lesson) => lesson.id));
  const [lessonAccess, userProgress] = await Promise.all([
    Promise.all(lessonIds.map(async (lessonId) => [lessonId, await canAccessLesson(authenticated?.user.id ?? null, lessonId)] as const)),
    authenticated ? listLessonProgressByLessonIds(authenticated.user.id, lessonIds) : [],
  ]);
  const accessByLessonId = new Map(lessonAccess);
  const progressByLessonId = new Map(userProgress.map((item) => [item.lessonId, item]));
  const outcomes = strings(course.learningOutcomes);
  const prerequisites = strings(course.prerequisites);

  return <main className="mx-auto max-w-5xl px-6 py-12"><Link href={`/courses/categories/${course.category.slug}`} className="text-sm font-semibold text-blue-700 hover:underline">← {course.category.title}</Link><header className="mt-5 rounded-2xl border border-slate-200 bg-white p-8 shadow-sm"><div className="flex flex-wrap gap-2 text-sm font-semibold"><span className="rounded-full bg-blue-50 px-3 py-1 text-blue-800">{course.level.code}</span><span className="rounded-full bg-slate-100 px-3 py-1 text-slate-700">{course.category.title}</span><span className="rounded-full bg-amber-50 px-3 py-1 text-amber-800">{course.accessPlan === "FREE" ? "Free" : course.accessPlan === "PREMIUM" ? "Premium" : "Corporate"}</span></div><h1 className="mt-4 text-4xl font-bold text-slate-900">{course.title}</h1><p className="mt-4 max-w-3xl text-slate-700">{course.fullDescription ?? course.shortDescription}</p><p className="mt-5 text-sm text-slate-500">{course.lessonCount} lessons · {course.estimatedDuration || "—"} minutes</p>{outcomes.length || prerequisites.length ? <div className="mt-6 grid gap-5 md:grid-cols-2">{outcomes.length ? <section><h2 className="font-semibold text-slate-900">What you will learn</h2><ul className="mt-2 list-disc space-y-1 pl-5 text-sm text-slate-700">{outcomes.map((item) => <li key={item}>{item}</li>)}</ul></section> : null}{prerequisites.length ? <section><h2 className="font-semibold text-slate-900">Prerequisites</h2><ul className="mt-2 list-disc space-y-1 pl-5 text-sm text-slate-700">{prerequisites.map((item) => <li key={item}>{item}</li>)}</ul></section> : null}</div> : null}</header><section className="mt-8 space-y-5" aria-label="Course modules">{course.modules.map((module) => <article key={module.id} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-2xl font-bold text-slate-900">{module.order}. {module.title}</h2>{module.description ? <p className="mt-2 text-slate-600">{module.description}</p> : null}<ol className="mt-5 space-y-3">{module.lessons.map((lesson) => { const access = accessByLessonId.get(lesson.id); const progress = progressByLessonId.get(lesson.id); const available = access?.allowed; const state = !available ? "Locked" : progress?.status === "COMPLETED" ? "Completed" : progress ? "In progress" : "Available"; const stateClass = state === "Completed" ? "bg-emerald-50 text-emerald-800" : state === "In progress" ? "bg-blue-50 text-blue-800" : state === "Locked" ? "bg-slate-100 text-slate-600" : "bg-slate-50 text-slate-700"; return <li key={lesson.id}>{available ? <Link href={`/courses/${course.slug}/lessons/${lesson.slug}`} className="flex min-h-14 items-center justify-between gap-4 rounded-xl border border-slate-200 px-4 py-3 text-slate-800 transition hover:border-blue-300 hover:bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-500"><span><span className="mr-2 text-sm font-semibold text-blue-700">{lesson.order}.</span>{lesson.title}</span><span className={`rounded-full px-3 py-1 text-xs font-semibold ${stateClass}`}>{state}{progress ? ` · ${progress.completionPercent}%` : ""}</span></Link> : <div className="flex min-h-14 items-center justify-between gap-4 rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-slate-500"><span><span className="mr-2 text-sm font-semibold">{lesson.order}.</span>{lesson.title}</span><span className={`rounded-full px-3 py-1 text-xs font-semibold ${stateClass}`}>🔒 {access?.reason === "AUTH_REQUIRED" ? "Sign in" : "Locked"}</span></div>}</li>; })}</ol></article>)}</section></main>;
}
