import Link from "next/link";
import { listVocabularyWordsForAdmin } from "@/modules/vocabulary/services/vocabulary.service";

export default async function AdminVocabularyPage({ searchParams }: { searchParams: Promise<{ q?: string }> }) {
  const query = (await searchParams).q;
  const words = await listVocabularyWordsForAdmin(query);
  return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-bold">Vocabulary CMS</h1><p className="mt-2 text-slate-600">Central, reusable lexical entries. Used entries are deactivated rather than deleted.</p></div><div className="flex gap-3"><Link href="/admin/vocabulary/import" className="rounded-lg border border-slate-300 px-4 py-2 font-semibold hover:bg-slate-50">Import</Link><Link href="/admin/vocabulary/new" className="rounded-lg bg-blue-700 px-4 py-2 font-semibold text-white hover:bg-blue-800">New word</Link></div></div><form className="mt-6 flex max-w-xl gap-2"><input name="q" defaultValue={query} placeholder="Search lemma or translation" className="min-w-0 flex-1 rounded-lg border border-slate-300 px-3 py-2" /><button className="rounded-lg border border-slate-300 px-4 py-2 font-semibold hover:bg-slate-50">Search</button></form><ul className="mt-6 grid gap-3 md:grid-cols-2">{words.map((word) => <li key={word.id}><Link href={`/admin/vocabulary/${word.id}`} className="block rounded-xl border border-slate-200 bg-white p-4 shadow-sm hover:border-blue-300 hover:bg-blue-50"><div className="flex justify-between gap-4"><div><p className="font-bold text-slate-900">{word.lemma}</p><p className="mt-1 text-sm text-slate-600">{word.meanings[0]?.translation ?? word.meanings[0]?.definition ?? "No meaning"}</p></div><span className={`h-fit rounded-full px-2 py-1 text-xs font-semibold ${word.isActive ? "bg-emerald-50 text-emerald-700" : "bg-slate-100 text-slate-600"}`}>{word.isActive ? "Active" : "Inactive"}</span></div><p className="mt-3 text-xs text-slate-500">{word.partOfSpeech ?? "No POS"} · {word.cefrLevel ?? "No CEFR"} · {word._count.lessonVocabulary} lesson links · {word._count.userWords} learners</p></Link></li>)}</ul>{words.length === 0 ? <p className="mt-6 rounded-xl border border-dashed border-slate-300 p-6 text-slate-600">No words found.</p> : null}</div>;
}
