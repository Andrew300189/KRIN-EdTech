import { NextRequest, NextResponse } from "next/server";
import { z } from "zod";
import { prisma } from "@/core/server/prisma";
import { requireContentManager } from "@/modules/courses/server/content-access";

const id = z.string().trim().min(1).max(191);
const productSchema = z.object({ code: z.string().trim().min(3).max(80).regex(/^[A-Z0-9_]+$/), slug: z.string().trim().min(3).max(120), title: z.string().trim().min(2).max(160), description: z.string().trim().max(1000).optional(), type: z.enum(["SUBSCRIPTION_PLAN", "COURSE", "COURSE_BUNDLE", "MODULE", "LESSON_PACK"]), planId: id.optional().nullable(), courseId: id.optional().nullable(), moduleId: id.optional().nullable(), isPublic: z.boolean().default(true) });
export async function GET(request: NextRequest) { const guard = await requireContentManager(request); if (!guard.ok) return NextResponse.json({ error: guard.error }, { status: guard.status }); return NextResponse.json({ products: await prisma.product.findMany({ include: { prices: true, plan: true, course: { select: { title: true } }, module: { select: { title: true } } }, orderBy: { createdAt: "desc" } }) }); }
export async function POST(request: NextRequest) { const guard = await requireContentManager(request); if (!guard.ok) return NextResponse.json({ error: guard.error }, { status: guard.status }); const value = productSchema.safeParse(await request.json().catch(() => null)); if (!value.success) return NextResponse.json({ error: "Invalid product." }, { status: 400 }); try { const product = await prisma.product.create({ data: value.data }); await prisma.contentAuditLog.create({ data: { actorId: guard.user.id, action: "PRODUCT_CREATED", entityType: "Product", entityId: product.id } }); return NextResponse.json({ product }, { status: 201 }); } catch { return NextResponse.json({ error: "Product code or slug already exists." }, { status: 409 }); } }
